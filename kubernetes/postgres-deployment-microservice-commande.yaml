apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-commande
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-commande
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-commande
  template:
    metadata:
      labels:
        app: postgres-commande
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: service2db
            - name: POSTGRES_USER
              value: service2user
            - name: POSTGRES_PASSWORD
              value: service2pass
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc-commande
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service-commande
spec:
  selector:
    app: postgres-commande
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
